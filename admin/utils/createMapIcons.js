// admin/utils/createMapIcons.js
// Run this once to create placeholder icons
const fs = require('fs');
const path = require('path');

// Create simple placeholder icons
const createPlaceholderImage = (color, text) => {
  // This is a simplified example - you'd need a proper image creation library
  console.log(`Create ${text} icon with color ${color}`);
};

// Or download the actual icons from:
// https://unpkg.com/leaflet@1.9.4/dist/images/