# d-riders-admin (starter)
Minimal Next.js admin dashboard.

Quick start with pnpm:
1. cd admin
2. pnpm install
3. set NEXT_PUBLIC_BACKEND if backend is remote (default http://localhost:4000)
4. pnpm run dev

The admin proxies to backend endpoints for riders and ping.
